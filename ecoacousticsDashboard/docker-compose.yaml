version: '3'
services:
  app:
    container_name: ${BRANCH_NAME}
    build:
      context: .
      dockerfile: ./Dockerfile_${BRANCH_NAME}
    ports:
      - ${PORT}:${PORT}
    working_dir: /code
    volumes:
      - ../${BRANCH_NAME}_data:/data
