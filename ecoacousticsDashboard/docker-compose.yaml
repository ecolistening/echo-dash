services:
  app:
    build:
      context: .
      dockerfile: ./Dockerfile
    ports:
      - ${PORT}:${PORT}
    working_dir: /code
    volumes:
      - ../data:/data
      - ../log:/log
    command: uv run --with gunicorn gunicorn --bind 0.0.0.0:${PORT} --timeout=800 app:server
