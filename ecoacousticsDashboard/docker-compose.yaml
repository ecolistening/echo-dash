services:
  app:
    container_name: ${BRANCH_NAME}
    build:
      context: .
      dockerfile: ./Dockerfile_${BRANCH_NAME}
    ports:
      - ${PORT}:${PORT}
    working_dir: /code
    volumes:
      - ../data/${BRANCH_NAME}/data:/data
      - ../log:/log
